numero1 = 0.0
numero2 = 0.0

numero1 = float(numero1)
numero2 = float(numero2)

numero1 = input('INGRESE EL PRIMER VALOR = ')
numero2 = input('INGRESE EL SEGUNDO VALOR = ')

sumado = 0.0
sumado = float(sumado)
restado=0.0
multiplicado=0.0
dividido=0.0

sumado = float(numero1) + float(numero2)

print('Sumado=',sumado)

restado = float(numero1) - float(numero2)

print('Restado=',restado)

multiplicado = float(numero1) * float(numero2)

print('Multiplicado=',multiplicado)

dividido = float(numero1) / float(numero2)

print('Dividido=',dividido)
